import Vue from 'vue';

export const DirectoryServiceTable = Vue.component('directory-services-table', require('./DirectoryServiceTable.vue'));
export const Listing = Vue.component('directory-services-listing', require('./Listing.vue'));
